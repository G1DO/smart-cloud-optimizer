# Core
boto3>=1.34.0
# Numeric/ML stack pinned to a known-good, mutually-compatible combo (matches the
# tested dev env). numpy is held at 1.x: numpy 2.x breaks pmdarima's compiled
# Cython ABI ('numpy.dtype size changed'). Bump these together, deliberately.
numpy==1.26.4
scipy==1.17.1
pandas==3.0.3

# ML
prophet==1.3.0
statsmodels==0.14.6
pmdarima==2.1.1

# Optimization
pulp>=2.7.0

# AI
openai>=1.10.0
google-genai>=1.63.0

# API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
httpx>=0.27.0

# Legacy Streamlit dashboard / local analysis UI
streamlit>=1.30.0
plotly>=5.18.0

# Utilities
python-dotenv>=1.0.0
tenacity>=8.2.0
matplotlib>=3.8.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
